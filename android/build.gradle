// android/build.gradle  – top-level
plugins {
    id("com.android.application") version "8.2.2" apply false
    id("com.android.library")    version "8.2.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false
    id("com.facebook.react")                             // ← permanece aplicado
}

/*  🔧 VARIABLES que necesitan los módulos nativos heredados  */
ext {
    compileSdkVersion = 34
    targetSdkVersion  = 34
    minSdkVersion     = 24
}

tasks.register("clean", Delete) { delete(rootProject.buildDir) }
afterEvaluate {
    tasks.matching { it.name.startsWith("prepareReanimatedHeadersForPrefabs") }
        .configureEach { doNotTrackState("Win + Gradle 8 work-around") }
}
